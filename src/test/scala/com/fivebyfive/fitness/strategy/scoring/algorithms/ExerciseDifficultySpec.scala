package com.fivebyfive.fitness.strategy.scoring.algorithms

import com.fivebyfive.fitness.BaseSpec

class ExerciseDifficultySpec extends BaseSpec {
  describe("ExerciseDifficulty") {
    describe("score") {
      it("scores a workout with an exercise over the given level a 0") {
      }
    }
  }
}
